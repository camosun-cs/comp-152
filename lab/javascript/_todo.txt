sub-lists need style to remove bottom margin
Detail disclosures need to be added everywhere
Algorithm puzzle needs a lot of style work.
	should probably bring it out of the section completely and make it its own thing
This lab in general is way too long. We should cut task 1 completely and have the puzzle automatically run the algorithm in the page. The lab should be entirely about creating the algorithm and nothing else.
